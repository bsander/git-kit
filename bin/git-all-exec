#!/bin/bash -eE
#
# Execute any command on this and all subrepos
#
# Usage: git all-exec <command>

echo
echo ---- Submodules
(set -x; git submodule foreach "echo ----; $* || :")
echo ---- Main repository
eval "set -x;" "$@"
